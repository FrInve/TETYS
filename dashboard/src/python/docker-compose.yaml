version: '3.1'

services:
  topic_search:
    build:
      context: ./
      dockerfile: Dockerfile
    image: topic_search:latest
    container_name: topic_app
    ports:
      - 8170:8501
    restart: unless-stopped
